server:
  port: 8081

spring:
  application:
    name: auth-resource
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9000
#          jwk-set-uri: http://localhost:9000/oauth/jwks